# Native app icons

`cargo tauri build` needs `icon.ico`, `32x32.png`, `128x128.png`, and
`tray.png`. These are generated by CI from `../../../src/assets/`
brand mark via `cargo tauri icon <svg>`. See
`docs/desktop-bridge-windows-build.md`. They are NOT committed to
avoid stale binary drift.